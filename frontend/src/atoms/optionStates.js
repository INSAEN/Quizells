import { atom } from "recoil";

export const option1State = atom({
  key: "option1State",
  default: false,
});
export const option2State = atom({
  key: "option2State",
  default: false,
});
export const option3State = atom({
  key: "option3State",
  default: false,
});
export const option4State = atom({
  key: "option4State",
  default: false,
});
